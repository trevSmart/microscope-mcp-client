{
	"name": "ibm-test-mcp-client",
	"version": "0.0.42",
	"main": "build/index.js",
	"type": "module",
	"bin": {
		"ibm-test-mcp-client": "build/index.js"
	},
	"files": [
		"build/**/*"
	],
	"config": {
		"mcpServer": "npx:@modelcontextprotocol/server-everything"
	},
	"scripts": {
		"lint:fix": "biome check . --write",
		"build": "npm run lint:fix &&tsc",
		"test": "npm run build && node scripts/test.mjs info",
		"test:oneshot": "npm run build && export LOG_LEVEL=debug && node scripts/test.mjs debug --call-tool 'salesforceMcpUtils {\"action\":\"getState\"}'",
		"publish-package": "npm version patch && npm run build && ./scripts/publish.sh"
	},
	"keywords": [
		"mcp",
		"client",
		"repl",
		"model-context-protocol"
	],
	"author": "Trevor Smart",
	"license": "ISC",
	"description": "MCP client for testing and interacting with MCP servers",
	"repository": {
		"type": "git",
		"url": ""
	},
	"engines": {
		"node": ">=18.0.0"
	},
	"dependencies": {
		"@anthropic-ai/sdk": "^0.60.0",
		"@modelcontextprotocol/sdk": "^1.17.4",
		"dotenv": "^17.2.1"
	},
	"devDependencies": {
		"@types/node": "^24.3.0",
		"globals": "^16.3.0",
		"@biomejs/biome": "^2.2.2",
		"typescript": "^5.9.2"
	}
}
