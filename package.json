{
	"name": "ibm-test-mcp-client",
	"version": "0.0.49",
	"main": "build/index.js",
	"type": "module",
	"bin": {
		"ibm-test-mcp-client": "build/index.js"
	},
	"files": [
		"build/**/*",
		"examples/**/*"
	],
	"scripts": {
		"build": "npm run lint &&tsc",
		"lint": "biome check . --write",
		"run:cli": "npm run build && node build/index.js",
		"test:cli": "npm run build && node scripts/test.mjs --automated",
		"test:1shot": "npm run build && node scripts/test.mjs --oneshot",
		"test:lib": "npm run build && node scripts/test-library.mjs",
		"publish-package": "npm run build && ./scripts/publish.sh"
	},
	"keywords": [
		"mcp",
		"client",
		"repl",
		"model-context-protocol"
	],
	"author": "Trevor Smart",
	"license": "ISC",
	"description": "MCP client for testing and interacting with MCP servers",
	"repository": {
		"type": "git",
		"url": ""
	},
	"engines": {
		"node": ">=18.0.0"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.17.4",
		"dotenv": "^17.2.1"
	},
	"devDependencies": {
		"@types/node": "^24.3.0",
		"globals": "^16.3.0",
		"@biomejs/biome": "^2.2.2",
		"typescript": "^5.9.2"
	}
}
